Recap of previous time:

What makes a convolution 2D is the number of axis in which it moves.

N.B. Pooling, by itself, does not downsample anything. To make the downsampling work we need to use padding, dilation and stride (as if stride is 0 the image doesn't get downsampled).

---

